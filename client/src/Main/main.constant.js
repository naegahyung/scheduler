const fiveMinutes = 4; // width of 5 minute interval
const oneHour = fiveMinutes * 12; // width of one hour interval
const row = oneHour * 14;
const height = 25;
const leftPadding1 = 20;
const leftPadding2 = 700;
const headerHeight = 40;

export {
  fiveMinutes,
  oneHour,
  row,
  height,
  leftPadding1,
  leftPadding2,
  headerHeight,
};