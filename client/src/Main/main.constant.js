const fiveMinutes = 2.5; // width of 5 minute interval
const oneHour = fiveMinutes * 12; // width of one hour interval
const row = oneHour * 14;
const height = 20;
const leftPadding1 = 150;
const leftPadding2 = 600;
const headerHeight = 40;

export {
  fiveMinutes,
  oneHour,
  row,
  height,
  leftPadding1,
  leftPadding2,
  headerHeight,
};